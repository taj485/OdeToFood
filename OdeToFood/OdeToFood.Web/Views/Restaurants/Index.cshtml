@model IEnumerable<OdeToFood.Data.Models.RestaurantModel>
@{
    ViewBag.Title = "Index";
}

<h2>Restaurants</h2>

<p>@Html.ActionLink("Create","Create")</p>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

    @foreach (var restaurant in Model)
    {
        <tr>
            <td>@Html.DisplayFor(modelrestaurant => restaurant.Name )</td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = restaurant.Id }) |
                @Html.ActionLink("Details", "Details", new { id = restaurant.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = restaurant.Id }) 
            </td>
        </tr>
    }
</table>

